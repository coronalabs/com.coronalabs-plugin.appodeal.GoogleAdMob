Sources for the plugin `plugin.appodeal.GoogleAdMob`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.GoogleAdMob" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
